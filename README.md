# factory-e2e-verify-build

Throwaway repository used once to exercise MadeThis PR #1280 (verify_build
installs a bare clone's dependencies) inside a real E2B coding sandbox.
It is deliberately shaped like a provisioned storefront: Next.js + Convex +
TypeScript, with a committed lockfile and no node_modules.

Safe to delete.
